database(correr, verbo_inf(intr)).
database(correr, verbo_inf(tra_i)).
database(sorrir, verbo_inf(intr)).
database(bater, verbo_inf(intr)).
database(bater, verbo_inf(tra_i)).

database(a, deter(f, s)).
database(menina, subst(f, s)).
database(floresta, subst(f, s)).
database(corre, verbo(s, correr)).
database(sorri, verbo(s, sorrir)).
database(para, prep).
database(mae, subst(f, s)).
database(vida, subst(f, s)).
database(o, deter(m, s)).
database(tempo, subst(m, s)).
database(cacador, subst(m, s)).
database(correu, verbo(s, correr)).
database(com, prep).
database(os, deter(m, p)).
database(lobos, subst(m, p)).
database(noticia, subst(f, s)).
database(pela, prep_det(f, s)).
database(cidade, subst(f, s)).
database(as, deter(f, p)).
database(lagrimas, subst(f, p)).
database(corriam, verbo(p, correr)).
database(pelo, prep_det(m, s)).
database(rosto, subst(m, s)).
database(rio, subst(m, s)).
database(mar, subst(m, s)).
database(bateu, verbo(s, bater)).
database(a, prep_det(f, s)).
database(porta, subst(f, s)).
database(vento, subst(m, s)).
database(na, prep_det(f, s)).
database(no, prep_det(m, s)).
database(martelo, subst(m, s)).
database(cachorro, subst(m, s)).
database(tambor, subst(m, s)).
database(tambores, subst(m, p)).
database(bateram, verbo(p, bater)).
database(sino, subst(m, s)).
